steps:
  - name: gcr.io/cloud-builders/docker
    args: ["build", "-t", "${_IMAGE}", "-f", "deploy/Dockerfile.api", "."]
images:
  - ${_IMAGE}
substitutions:
  _IMAGE: gcr.io/${PROJECT_ID}/astraea-api
timeout: 1200s
